---
title: 'Overrides'
---

import PolicyDefinition from '/snippets/vocabulary/policy-definition.mdx';
import PolicySchemaDefinition from '/snippets/vocabulary/policy-schema-definition.mdx';
import OverrideDefinition from '/snippets/vocabulary/override-definition.mdx';

Thus far, we've defined config schemas, uploaded them to Miru, and stored configuration 
data in clients and tags. The next step is using overrides to format configuration data 
according to the config schema used by clients.

## Override
<OverrideDefinition />
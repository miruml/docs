---
title: 'Defining Policies'
---

import PolicyDefinition from '/snippets/policy-definition.mdx';
import PolicySchemaDefinition from '/snippets/policy-schema-definition.mdx';
import OverrideDefinition from '/snippets/override-definition.mdx';

## Policy
Thus far, we've defined config schemas, uploaded them to Miru, and stored configuration data in clients and tags. The next step is using policies to format configuration data according to the config schema used by clients.

<PolicyDefinition />

## Policy Schema
<PolicySchemaDefinition />

## Override
<OverrideDefinition />
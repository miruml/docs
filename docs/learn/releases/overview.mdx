---
title: "Overview"
---

import ReleaseDef from '/snippets/definitions/release.mdx';
import {Framed} from '/snippets/components/framed.jsx';
import {ImmutableBadge, EditableBadge} from '/snippets/components/badges.jsx';
import {CUSTOM_VALIDATION_TOOLTIP} from '/snippets/components/releases/cus-val.jsx';

<Framed background="https://assets.miruml.com/docs/v02/images/releases/background.jpeg" image="https://assets.miruml.com/docs/v02/images/releases/header:page.png" borderWidth="36px 0 0 56px" innerRadius="8px 0 0 0" />

<ReleaseDef />

## Properties

<ParamField path="version" type="string">
  <ImmutableBadge />

  A [semantic version](https://semver.org/), unique for a given release.

  Versions must be dot-separated integers. You may optionally use a `v` prefix, a prerelease suffix (e.g. `-alpha.X`, `-beta.X`, `-rc.X`), or a build suffix (e.g. `+build-metadata`).

  Examples: `v1`, `1`, `v2.1`, `2.1`, `v3.2.1`, `3.2.1`, `v4.3.2-beta.1`, `4.3.2-rc.1`, `v5.4.3+metadata`, `6.5.4-beta.2+metadata`
</ParamField>

<ParamField path="config schemas" type="[] Config Schema">
  <ImmutableBadge />

  The [config schemas](/docs/learn/schemas/overview#properties) that define compatible config instances for the release.

  Examples: `SCH-FmoDN`, `SCH-3JcLq`, `SCH-HHDRn`
</ParamField>
---
title: 'Push the Schema'
---

This guide assumes you've read the core concepts section on 
[Creating Schemas](/pages/core-concepts/creating-schemas#uploading-a-config-schema).

### Commit Changes
Let's navigate back to the [getting-started](https://github.com/miruml/getting-started) 
repository and terminal.
 The CLI needs 
a clean working directory to track schema versions properly.

If you've modified the repository after cloning it, commit your changes:

```bash
git add .
git commit -m "getting started guide"
```

### Push to Miru
Now that we've committed our changes, let's push the schema:

```bash
miru schema push ./schemas/mobility.schema.yaml
```

<Note>
    We're specifying the [file path](/pages/getting-started/starter-repo#configuration-schema) 
    to the schema, not the [config slug](/pages/getting-started/create-config#config-slug).
    For more details, see the [CLI Reference](/pages/cli-reference/schema-push).
</Note>

You'll see a confirmation message that your schema was uploaded.

```bash
📦 Attempting to push 1 config schemas...

🏁 Successfully pushed schema for mobility config
    commit: a8340d496013f4e33bb5d59fbebf057a80c44dbf
    branch: main
    origin: https://github.com/miruml/getting-started.git
    git file path: schemas/mobility.schema.yaml


✅ Successfully uploaded 1 config schemas
```

Visit the [Configs page](https://configs.miruml.com/configs) and click on 
`Robot Features` config to verify your schema was uploaded successfully.

<Frame>
    <img 
        src="/images/getting-started/config-schemas-page.png" 
        alt="Successful Schema Push" 
    />
</Frame>

<Warning>
    When viewing your schema in the Miru UI, clicking on the commit hash will take you 
    to that specific commit in your remote repository—but only if you own the repository. 
</Warning>
import JSONSchemaDescription from '/snippets/jsonschema-description.mdx';
import SchemaLanguages from '/snippets/schema-languages.mdx';

A **config schema** defines the structure, types, and constraints of a configuration. It serves as a contract between code and configuration, ensuring that config instances are valid before deployment and safe to consume at runtime.

<JSONSchemaDescription />

<SchemaLanguages />
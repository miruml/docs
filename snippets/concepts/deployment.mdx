A **deployment** is a set of config instances made available on a device for some interval of time.

A deployment adheres to a release in that it must have a valid config instance for each config schema in the release.
A **release** represents a version of your application software. In Miru, a release contains a set of [config schemas](/docs/home/basics/vocabulary#config-schema) that define the valid configurations for the software release it represents.

While a config schema defines valid parameters for a particular part of your application, a release defines valid parameters for your entire application.
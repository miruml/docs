To login, run the `login` command.

```bash
miru login
```

Retrieve your authentication token from the [Secrets](https://configs.miruml.com/settings/cli-token) page.

<Frame>
    <img src="/images/secrets/cli-token/page.png" style={{borderRadius: '16px'}} />
</Frame>

Paste the token into the CLI.

```
Please retrieve your authentication token from the following URL:
🔗 https://configs.miruml.com/settings/cli-token

🔑 Paste your authentication token: **********

Validating authentication token...
✅ Successfully logged in as Benjamin
```
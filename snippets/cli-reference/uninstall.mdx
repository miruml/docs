Remove the binary from your system.

<Tabs>
    <Tab title="Linux">
        ```bash
        sudo rm "$(which miru)"
        ```
    </Tab>

    <Tab title="macOS">
        ```bash
        brew uninstall miru
        ```
    </Tab>
</Tabs>

Remove any configuration files.

```bash
rm -rf ~/.miru
```


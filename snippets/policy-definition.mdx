A **policy** is a set of **overrides** that hierarchically evaluate to a **Concrete Config**. Policies have two components:
1. A policy schema
2. A hierarchical set of overrides

A **Concrete Config** must follow a specific **Config Schema** when deployed to a client. **Config Schemas** define a policy that evaluates the client’s tags to determine the appropriate **Concrete Config** for deployment.  

As an example, the config schema `v8` of the Motion Control config uses the `v4` Policy.

<img src="/images/core-concepts/motion-control-schema-list-zoomed.png" style={{borderRadius: '7px'}}/>